{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "type": "module",
  "main": "index.html",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "devDependencies": {
    "chai": "^5.1.1",
    "jest": "^29.7.0",
    "mocha": "^10.4.0",
    "nyc": "^15.1.0",
    "sinon": "^18.0.0"
  }
}
